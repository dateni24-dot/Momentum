// Placeholder test — la app usa Supabase y requiere inicialización async,
// por lo que los tests de integración se implementarán en SCRUM-18.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
